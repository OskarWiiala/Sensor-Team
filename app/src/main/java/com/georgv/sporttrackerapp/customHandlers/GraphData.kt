package com.georgv.sporttrackerapp.customHandlers

import android.util.Log

class GraphData(selectedVariable: String = "Distance", selectedTime: String = "Today") {
    // Will eventually fetch data from database
}